#include <stdio.h>
#include <cs50.h>


void bubbleSort(int arr[], int n);
void showArray(int arr[], int n);
int main(void){
    int arr[] = {4, 5, 1, 34, 2, 9, 3, 8, 0, 12};
    
    showArray(arr, 10);
    
    
    
    return 0;
   
}


void bubbleSort(int arr[], int n){
    int i, j;
    for( i = 0; i < n ; i++){
        for( j = 0; j )
    }
}

void showArray(int arr[], int n){
    for(int i = 0; i < n; i++){
        printf("%d: %d\n",i+1, arr[i]);
    }
}
